# REST API — gateway
NEXT_PUBLIC_GATEWAY_BASE=http://localhost:8080

# Socket.IO — realtime-service (Phase 10 cutover)
NEXT_PUBLIC_REALTIME_BASE=http://localhost:3333

# NextAuth Google OAuth
NEXTAUTH_URL=http://localhost:3000
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
JWT_SECRET=

# ImageKit CDN base (public key comes from gateway upload-init response)
NEXT_PUBLIC_IMAGEKIT_ENDPOINT=
